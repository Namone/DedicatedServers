version: "3"
services:
  barotrauma:
    # image: barotrauma:latest
    # build:
    #   context: .
    #   target: barotrauma-server
    # ports:
    #   - 27015:27015
    #   - 27016:27016
    # volumes:
    #   - ./barotrauma-data:/home/barotrauma-data
    # # command: ["/usr/games/steamcmd", "+force_install_dir /home/barotrauma-data", "+login anonymous", "+app_run 1026340"]
    # command: ["sleep", "3800"]
  zomboid:
    image: zomboid:latest
    build:
      context: .
      target: zomboid-server
    ports:
      - 16262:16262
    volumes:
      - ./zomboid-data:/home/zomboid-data
    command: ["/usr/games/steamcmd", "+force_install_dir /home/zomboid-data", "+login anonymous", "+app_run 380870"]